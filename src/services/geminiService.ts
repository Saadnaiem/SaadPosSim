// AI Service has been disabled/removed.
// This file is kept as a placeholder to prevent import errors in other files if references are missed,
// but logic has been cleared to avoid 'GoogleGenAI' dependency.

export const generateSmartCoupon = async (prompt: string): Promise<any> => {
  console.warn("AI Smart Assist is disabled in this version.");
  return null;
};